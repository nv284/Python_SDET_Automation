Day 5 - File Handling, Exceptions & Logging

This folder contains lessons, examples, a small project, and two assignments focused on:

- File operations: text, CSV, JSON read/write
- Test data management and fixtures
- Exception handling strategies and custom exceptions
- Logging: using `logging` module and simple framework patterns
- Basic debugging fundamentals and tips

Work through `lessons/lesson.md`, run examples in `examples/`, then try the `projects/` and `assignments.txt` tasks.
